import { Text } from '@chakra-ui/layout';

const AnswerPercentage = () => {
  return (
    <Text fontSize="large" p={5} backgroundColor="teal" mb="auto">
      0%
    </Text>
  );
};

export default AnswerPercentage;
